<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.Android_assessment_universities" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Android_assessment_universities" parent="Base.Theme.Android_assessment_universities" />

    <style name="TextViewMedium">
        <item name="android:textColor">@color/text_view_medium</item>
        <item name="android:textSize">@dimen/_11ssp</item>
    </style>
    <style name="progressBarBlue" parent="@style/Theme.AppCompat">
        <item name="colorAccent">@color/black</item>
    </style>
</resources>